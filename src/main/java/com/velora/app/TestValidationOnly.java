package com.velora.app;

import com.velora.app.core.utils.ValidationUtils;
import com.velora.app.core.utils.RegexPatterns;
import java.util.UUID;

public class TestValidationOnly {
    // This class previously contained an ad-hoc manual test harness for ValidationUtils.
    // The test logic should be maintained in proper unit tests under src/test/java instead
    // of in a standalone main method in production sources.
}
